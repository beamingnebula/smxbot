# Copy this file to `.env` and fill in the values.
# Do NOT commit your real `.env` to source control.

# Required: Telegram Bot token from @BotFather
BOT_TOKEN=8529193058:AAFLpLJhAERyWD78kxml7PSpCuG7HyOcMkA

# Optional: Bot username used when generating deep-links (without the @)
# Defaults to 'smxfilesbot' when not set.
BOT_USERNAME=smxfilesbot

# Optional: Number of days before a generated token expires (integer)
# Set to 0 to disable automatic expiration — links will never expire
# Defaults to 0 (never expire)
TOKEN_TTL_DAYS=0

# Optional: path to sqlite DB file (defaults to `file_links.db`)
DB_PATH=file_links.db

# Optional: logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
